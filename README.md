# 🤖 ИИ-помощник для баз знаний (Telegram Knowledge Bot)

Telegram-бот на Python для автоматической обработки документов с AI-суммаризацией и извлечением ключевых слов. Интегрируется с n8n/Make для автоматизации и сохраняет результаты в Google Sheets.

## Описание

Система принимает документы от пользователей через Telegram (файл или URL), извлекает текст с помощью специализированных парсеров, генерирует краткое описание (3-7 предложений) и ключевые слова (5-10) с помощью AI, и сохраняет результаты в Google Sheets.

### Возможности

- Поддержка форматов: PDF, DOCX, TXT, MD
- OCR для сканированных документов (Tesseract)
- AI-суммаризация (OpenRouter, OpenAI GPT, YandexGPT, Claude)
- Извлечение ключевых слов (YAKE)
- Сохранение в Google Sheets
- Загрузка документов по URL
- Определение языка (русский/английский)
- Подробное логирование операций

## Требования

### Системные требования

- Python 3.10+
- Tesseract OCR (для распознавания сканов)

### Зависимости Python

```
python-telegram-bot>=20.0
PyPDF2>=3.0.0
pdfplumber>=0.10.0
python-docx>=1.0.0
pytesseract>=0.3.10
Pillow>=10.0.0
openai>=1.0.0
yake>=0.4.8
gspread>=5.0.0
oauth2client>=4.1.3
python-dotenv>=1.0.0
chardet>=5.0.0
requests>=2.31.0
```

### Для разработки

```
hypothesis>=6.0.0
pytest>=7.0.0
pytest-asyncio>=0.21.0
```

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/meloch287/Telegram-Knowledge-Bot.git
cd telegram-knowledge-bot
```

### 2. Создание виртуального окружения

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/macOS
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка Tesseract OCR (опционально)

**Windows:**
1. Скачайте установщик с https://github.com/UB-Mannheim/tesseract/wiki
2. Установите и добавьте путь в переменную окружения `TESSERACT_PATH`

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install tesseract-ocr tesseract-ocr-rus
```

**macOS:**
```bash
brew install tesseract tesseract-lang
```

### 5. Настройка переменных окружения

```bash
cp .env.example .env
```

Отредактируйте `.env` файл с вашими настройками (см. раздел Конфигурация).

## Настройка Telegram бота

### Создание бота через BotFather

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "My Knowledge Bot")
4. Введите username бота (должен заканчиваться на `bot`, например: `my_knowledge_bot`)
5. Скопируйте полученный токен в `TELEGRAM_BOT_TOKEN`

### Настройка Webhook (для production)

1. Получите SSL-сертификат для вашего домена
2. Укажите `TELEGRAM_WEBHOOK_URL` в формате `https://your-domain.com/webhook/telegram`
3. Сгенерируйте секретный ключ для `TELEGRAM_WEBHOOK_SECRET`

### Режим Polling (для разработки)

Оставьте `TELEGRAM_WEBHOOK_URL` пустым — бот запустится в режиме polling.

## Настройка Google API

### 1. Создание проекта в Google Cloud Console

1. Перейдите на https://console.cloud.google.com/
2. Создайте новый проект или выберите существующий
3. Включите Google Sheets API и Google Drive API

### 2. Создание Service Account

1. Перейдите в IAM & Admin → Service Accounts
2. Нажмите "Create Service Account"
3. Введите имя и описание
4. Нажмите "Create and Continue"
5. Пропустите роли (Skip)
6. Нажмите "Done"

### 3. Создание ключа

1. Откройте созданный Service Account
2. Перейдите на вкладку "Keys"
3. Нажмите "Add Key" → "Create new key"
4. Выберите JSON формат
5. Сохраните файл в `credentials/google_service_account.json`

### 4. Настройка Google Sheets

1. Создайте новую таблицу в Google Sheets
2. Скопируйте ID таблицы из URL (между `/d/` и `/edit`)
3. Укажите ID в `GOOGLE_SHEET_ID`
4. Предоставьте доступ Service Account email к таблице (Editor)

### Структура таблицы (создаётся автоматически)

| Колонка | Описание |
|---------|----------|
| timestamp | Дата и время обработки |
| uploader_id | Telegram user ID |
| uploader_username | Telegram username |
| file_name | Имя файла |
| file_type | Тип файла (pdf/docx/txt/md) |
| file_size | Размер в байтах |
| char_count | Количество символов |
| language | Язык документа |
| summary | AI-суммаризация |
| keywords | Ключевые слова |
| status | Статус обработки |
| error_message | Сообщение об ошибке |
| ai_model_used | Использованная AI модель |
| extraction_method | Метод извлечения текста |
| ocr_used | Использовался ли OCR |
| processing_time | Время обработки (сек) |

## Конфигурация

Все настройки хранятся в файле `.env`. Основные параметры:

### Telegram
```env
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_WEBHOOK_URL=https://your-domain.com/webhook/telegram
TELEGRAM_WEBHOOK_SECRET=your_secret_key
```

### Google Sheets
```env
GOOGLE_SHEET_ID=your_spreadsheet_id
GOOGLE_CREDENTIALS_PATH=./credentials/google_service_account.json
GOOGLE_SHEET_NAME=Documents
```

### AI API

Поддерживаются провайдеры: `openai`, `openrouter`, `yandex`, `claude`

```env
AI_PROVIDER=openrouter
OPENAI_API_KEY=sk-or-v1-your-key
OPENAI_MODEL=google/gemini-2.0-flash-exp:free
```

### Настройка OpenRouter (рекомендуется)

OpenRouter позволяет использовать разные AI модели через единый API, включая бесплатные.

1. Зарегистрируйтесь на https://openrouter.ai
2. Перейдите в https://openrouter.ai/keys
3. Нажмите "Create Key" и скопируйте ключ
4. Укажите в `.env`:

```env
AI_PROVIDER=openrouter
OPENAI_API_KEY=sk-or-v1-ваш-ключ
OPENAI_MODEL=google/gemini-2.0-flash-exp:free
```

Бесплатные модели:
- `google/gemini-2.0-flash-exp:free`
- `meta-llama/llama-3.1-8b-instruct:free`
- `mistralai/mistral-7b-instruct:free`

Полный список моделей: https://openrouter.ai/models

### Лимиты обработки
```env
MAX_FILE_SIZE_MB=20
SUMMARY_MIN_SENTENCES=3
SUMMARY_MAX_SENTENCES=7
KEYWORDS_MIN_COUNT=5
KEYWORDS_MAX_COUNT=10
```

### Retry логика
```env
MAX_RETRIES=3
RETRY_BASE_DELAY=1.0
RETRY_MAX_DELAY=30.0
```

Полный список переменных см. в `.env.example`.

## Использование

### Запуск бота

```bash
python -m src.main
```

Или:

```bash
python src/main.py
```

### Команды бота

- `/start` — Приветствие и инструкции
- `/help` — Справка по использованию

### Отправка документов

1. **Файл как вложение**: Просто отправьте файл (PDF, DOCX, TXT, MD) боту
2. **URL на документ**: Отправьте ссылку на файл

### Пример работы

1. Пользователь отправляет PDF файл
2. Бот отвечает: "⏳ Обработка документа..."
3. Система извлекает текст из PDF
4. AI генерирует суммаризацию (3-7 предложений)
5. YAKE извлекает ключевые слова (5-10)
6. Результаты сохраняются в Google Sheets
7. Бот отправляет результат пользователю:

```
✅ Документ обработан!

📝 Суммаризация:
Документ описывает архитектуру системы обработки данных. 
Рассматриваются основные компоненты и их взаимодействие.
Приводятся примеры использования API.

🏷️ Ключевые слова:
архитектура, API, система, компоненты, интеграция
```

## 📁 Структура проекта

```
telegram-knowledge-bot/
├── src/
│   ├── __init__.py
│   ├── config.py              # Загрузка конфигурации
│   ├── main.py                # Entry point приложения
│   ├── processor.py           # Главный класс обработки
│   ├── ai/
│   │   ├── __init__.py
│   │   ├── base.py            # Абстрактный класс AI
│   │   ├── keyword_extractor.py  # Извлечение ключевых слов (YAKE)
│   │   ├── language_detector.py  # Определение языка
│   │   └── openai_summarizer.py  # OpenAI суммаризатор
│   ├── bot/
│   │   ├── __init__.py
│   │   ├── handlers.py        # Обработчики Telegram
│   │   ├── notifications.py   # Сервис уведомлений
│   │   └── validators.py      # Валидаторы для бота
│   ├── models/
│   │   ├── __init__.py
│   │   ├── config.py          # Модели конфигурации
│   │   ├── enums.py           # Перечисления
│   │   ├── metadata.py        # Метаданные документа
│   │   ├── results.py         # Результаты обработки
│   │   └── storage.py         # Модели для Google Sheets
│   ├── parsers/
│   │   ├── __init__.py
│   │   ├── base.py            # Абстрактный парсер
│   │   ├── docx_parser.py     # Парсер DOCX
│   │   ├── factory.py         # Фабрика парсеров
│   │   ├── md_parser.py       # Парсер Markdown
│   │   ├── ocr_engine.py      # OCR движок
│   │   ├── pdf_parser.py      # Парсер PDF
│   │   └── txt_parser.py      # Парсер TXT
│   ├── storage/
│   │   └── __init__.py        # Google Sheets storage
│   └── utils/
│       ├── __init__.py
│       ├── logger.py          # Логирование операций
│       ├── retry_handler.py   # Retry с exponential backoff
│       └── validators.py      # Валидация файлов и URL
├── tests/
│   ├── __init__.py
│   ├── property/              # Property-based тесты
│   │   ├── test_file_validation_properties.py
│   │   ├── test_keyword_properties.py
│   │   ├── test_logger_properties.py
│   │   ├── test_metadata_properties.py
│   │   ├── test_parser_properties.py
│   │   ├── test_storage_properties.py
│   │   ├── test_summarizer_properties.py
│   │   └── test_workflow_properties.py
│   └── unit/                  # Unit тесты
├── workflows/
│   └── n8n_workflow.json      # Экспортируемый n8n workflow
├── credentials/               # Google API credentials
├── logs/                      # Логи приложения
├── .env.example               # Шаблон переменных окружения
├── requirements.txt           # Зависимости Python
└── README.md                  # Документация
```

## n8n Workflow

Проект включает готовый workflow для n8n в `workflows/n8n_workflow.json`.

### Импорт workflow

1. Откройте n8n
2. Перейдите в Workflows → Import from File
3. Выберите `workflows/n8n_workflow.json`
4. Настройте credentials для каждого node

### Структура workflow

```
Telegram Trigger → File Handler → Parser Router → AI Summarizer → 
Keyword Extractor → Google Sheets → Telegram Notify
```

С обработкой ошибок и retry логикой на каждом этапе.

## Тесты

### Запуск всех тестов

```bash
pytest
```

### Запуск property-based тестов

```bash
pytest tests/property/ -v
```

### Запуск конкретного теста

```bash
pytest tests/property/test_parser_properties.py -v
```

## Troubleshooting

### Бот не отвечает

1. Проверьте правильность `TELEGRAM_BOT_TOKEN`
2. Убедитесь, что бот запущен (`python -m src.main`)
3. Проверьте логи в `logs/processing.log`

### Ошибка "Unsupported format"

Поддерживаются только форматы: PDF, DOCX, TXT, MD. Убедитесь, что файл имеет правильное расширение.

### Ошибка "File too large"

Максимальный размер файла — 20MB. Уменьшите размер файла или измените `MAX_FILE_SIZE_MB`.

### OCR не работает

1. Убедитесь, что Tesseract установлен
2. Проверьте путь в `TESSERACT_PATH`
3. Установите языковые пакеты: `tesseract-ocr-rus`

### Ошибка Google Sheets

1. Проверьте `GOOGLE_CREDENTIALS_PATH`
2. Убедитесь, что Service Account имеет доступ к таблице
3. Проверьте правильность `GOOGLE_SHEET_ID`

### Ошибка OpenAI API

1. Проверьте `OPENAI_API_KEY`
2. Убедитесь, что на аккаунте есть кредиты
3. Проверьте лимиты API

### Логи

Все операции логируются в `logs/processing.log` в JSON формате:

```json
{
  "timestamp": "2024-12-12T14:30:00Z",
  "event_type": "file_upload",
  "details": {
    "file_name": "document.pdf",
    "file_size": 1548576,
    "uploader_id": 123456789
  }
}
```

## Лицензия

MIT License
